release:

builds:
- id: bb-cli
  main: main.go
  binary: bb-cli
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64
  env:
  - CGO_ENABLED=0

archives:
- builds:
  - bb-cli
  replacements:
    darwin: Darwin
    linux: Linux
    windows: Windows
    amd64: x86_64
  format: tar.gz
  format_overrides:
    - goos: windows
      format: zip

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'

nfpms:
  - license: MIT
    maintainer: Bitbull
    description: Bitbull CLI
    homepage: https://github.com/daaru00/bb-cli/
    suggests:
      - awscli
      - session-manager-plugin
    formats:
      - rpm
      - deb
